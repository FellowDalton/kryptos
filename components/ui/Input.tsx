/**
 * Input component for text entry
 * Clean, minimal styling with focus states
 * TODO: Implement with proper styling, validation, and accessibility
 */
export default function Input() {
  return <input type="text" placeholder="Input" />;
}
